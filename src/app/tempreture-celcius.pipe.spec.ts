import { TempretureCelciusPipe } from './tempreture-celcius.pipe';

describe('TempretureCelciusPipe', () => {
  it('create an instance', () => {
    const pipe = new TempretureCelciusPipe();
    expect(pipe).toBeTruthy();
  });
});
