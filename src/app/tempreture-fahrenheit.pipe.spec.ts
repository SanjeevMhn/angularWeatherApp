import { TempretureFahrenheitPipe } from './tempreture-fahrenheit.pipe';

describe('TempretureFahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new TempretureFahrenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
